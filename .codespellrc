[codespell]
skip = node_modules,reports,.svelte-kit,build,dist,bun.lock,package-lock.json,*.lock,*.min.js,*.min.css,id.ts

# Indonesian locale false positives — valid Bahasa Indonesia words
ignore-words-list =
  afterAll,
  re-use

# Only check source files
check-filenames = false
